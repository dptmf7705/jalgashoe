<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">
        <data>
                <import type="android.view.View"/>
                <variable
                    name="activity"
                    type="com.dankook.jalgashoe.BaseActivity"/>
                
        </data>
        <RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:paddingLeft="16dp"
            android:paddingRight="16dp">
        
                <!--<ImageButton
                    android:id="@+id/button_left"
                    android:layout_width="35dp"
                    android:layout_height="35dp"
                    android:layout_centerVertical="true"
                    android:background="@android:color/transparent"
                    android:visibility="@{activity.leftImageId == 0 ? View.INVISIBLE : View.VISIBLE}"
                    app:imageId="@{activity.leftImageId}"/>
        
                <ImageView
                    android:paddingTop="5dp"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_centerInParent="true"
                    android:adjustViewBounds="true"
                    android:src="@drawable/icon_appname"
                    android:scaleType="centerCrop"/>
        
                <ImageButton
                    android:id="@+id/button_right"
                    android:layout_width="35dp"
                    android:layout_height="35dp"
                    android:layout_centerVertical="true"
                    android:layout_alignParentRight="true"
                    android:layout_alignParentEnd="true"
                    android:background="@android:color/transparent"
                    android:visibility="@{activity.rightImageId == 0 ? View.INVISIBLE : View.VISIBLE}"
                    app:imageId="@{activity.rightImageId}"/>
        -->
        </RelativeLayout>
</layout>